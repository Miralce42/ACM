#include<iostream>
#include<map>
#include<algorithm>

using namespace std;

const int maxn = 1000 + 100;

map<int,int> midpoint,sum,cnt;

int main()
{
	int n,num,row[maxn],col,sum;
	cin >> n;
	while(n--)
	{
		cin >> m;
		for(int i = 0 ; i < m ; i++)
		{
			cin << col << row[i];
			if(sum.count(row[i]))
				sum[row[i]] == 0;
			if(cnt.count(row[i]))
				cnt[row[i]] == 0;
			sum[row[i]] += col;
			cnt[row[i]]++;
		}
		for
		for(int r = 0 ; r < maxn ;r++)
		{
			sum = 0;
			for(int c = 0 ; c < point[r].size() ;c++)
			{
				sum += point[r][c]
			}
			midpoint[maxn]
		}
	}
	return 0;
}